# sEMG 手势识别流水线诊断（基于当前仓库可见代码）

## 结论（当前置信度不足 96%）
目前仓库中只有采集前端 `emg_v9_updated.html`，未包含你提到的 `pre_processing.py`、`cnn_tune.py`、`predict_interactive.py` 以及数据集 CSV 文件，因此无法达到你要求的 96% 置信度来精确定位“唯一故障步骤”。

## 已确认的高风险点（按概率排序）
1. **训练/预测标签映射可能错位（最高风险）**
   - 采集端动作标签是 `0..6`（7 类），但同时混入 `-4`（放松/准备）、`-2`（动作过渡）、`-3`（作废段）。
   - 如果 `pre_processing.py` 未严格过滤 `Label < 0`，CNN 会学到“过渡/休息”噪声，准确率会显著下降。

2. **动作有效段定义与切窗策略可能不一致**
   - 采集端每个动作 6 秒，其中只有中间 4 秒打上有效标签（前后 1 秒为 `-2` 过渡）。
   - 若预处理按整段 6 秒切窗而非仅保留有效 4 秒，会引入大量边界噪声。

3. **通道 4 频带与其他通道不一致**
   - 固件逻辑（你贴的 C 代码）里 CH4 使用 250Hz 低通，CH1~CH3 使用 400Hz 低通。
   - 若模型假设 4 通道同分布，这种带宽差异会导致通道间统计特性偏移，影响泛化。

4. **串口 CSV 实时流存在丢包风险但前端无显式完整性校验**
   - 前端仅按 `\n` 与 `,` 解析并在 `parts.length < 4` 时丢弃，未记录丢行率。
   - 训练集若包含间歇丢点、错行，将造成时间窗破损。

## 你下一步应先做的 3 件事
1. 在预处理里打印标签直方图（过滤前/后各一次），确认最终训练标签只包含 `0..6`。
2. 对每个样本窗附加 `source_segment`（rest/prep/action-transition/action-valid）调试字段，验证训练集是否只来自 action-valid。
3. 分通道做 PSD 或带宽统计，确认 CH4 与 CH1~CH3 的分布差异是否过大；必要时统一滤波策略或在模型前做通道标准化。

## 为达到 96% 置信度必须补充的文件
请提供以下 4 个文件（至少贴关键函数）：
- `pre_processing.py`（标签过滤、切窗、标准化、数据集划分）
- `cnn_tune.py`（模型输入形状、类别数、loss、class weights、验证划分策略）
- `predict_interactive.py`（在线窗口长度、步长、与训练一致性）
- 你当前使用的 CSV 样本（前 200 行 + 标签分布统计）

补齐后我可以给出：
- 具体“哪一步错了”的单点结论（而不是概率排序）
- 最小修改补丁（逐行建议）
- 预期准确率提升区间
